import type { Metadata } from "next";
import "./globals.css";
import React from "react";
import Header from "@/components/common/Header";

export const metadata: Metadata = {
  title: "Nabin Katwal",
  description: "Hello!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`antialiased max-w-[1300px] text-[#FAF5F6] bg-[#252525] `}
      >
        <Header />
        <div className={"max-w-[1300px] mx-auto px-6 sm:px-8 md:px-12 lg:px-16 xl:px-20 2xl:px-24"} >
            {children}
        </div>
      </body>
    </html>
  );
}
